# Countdown_Clock
Using a Nightmare Before Christmas Holiday countdown clock, this project adds 7-segment displays, a ESP32 and static register shift module to display a target day in the future.  Optionally a string of LEDs can be used to give a background glow.

The target date is set via web page accessed over the users home Wi-Fi network.
